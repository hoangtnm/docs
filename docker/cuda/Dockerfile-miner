FROM nvidia/cuda:10.2-devel-ubuntu18.04

RUN apt-get update && apt-get install -y \
    build-essential \
    cmake \
    git \
    gcc \
    g++ \
    perl \
    libdbus-1-dev \
    libleveldb-dev \
    libjsoncpp-dev \
    libjsonrpccpp-dev \
    libboost-all-dev \
    libgmp-dev \
    libreadline-dev \
    libmicrohttpd-dev \
    mesa-common-dev \
    ocl-icd-libopencl1 \
    opencl-headers \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /t-rex
RUN wget -O t-rex.tar.gz https://github.com/trexminer/T-Rex/releases/download/0.24.7/t-rex-0.24.7-linux.tar.gz \
    && tar -zxf t-rex.tar.gz

ENTRYPOINT [ "/t-rex/t-rex" ]
CMD [ "--help" ]
